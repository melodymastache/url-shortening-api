$(document).ready(() => {
  $(".burger").click(() => {
    $(".dropdown").toggle(); // event handler for mobile menu
  });

  $(".shorten").click
  var empty = "";
  var link = document.getElementsByClassName("textbox");
  if (link === empty) {
    $("[type='text']").focus();
  }
});